console.log("Testing Supabase connection..."); const { createClient } = require("@supabase/supabase-js"); require("dotenv").config({ path: ".env.local" }); const url = process.env.NEXT_PUBLIC_SUPABASE_URL; const key = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY; console.log("URL:", url); console.log("Key present:", !!key); if (url && key) { const supabase = createClient(url, key); supabase.auth.getSession().then(r => console.log("Connection test result:", r.error ? "Error: " + r.error.message : "Success")).catch(e => console.log("Connection failed:", e.message)); }
