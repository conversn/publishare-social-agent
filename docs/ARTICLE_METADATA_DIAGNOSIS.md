# Article Metadata Generation - Diagnosis & Fix Plan

## Issues Identified

### ✅ Working Fields
- `title`, `content`, `excerpt` - Generated correctly
- `meta_title`, `meta_description` - Set from title/excerpt
- `aeo_summary`, `aeo_content_type`, `schema_markup` - AEO fields populated
- `featured_image_url` - Generated for majority of articles

### ❌ Missing Fields

#### Critical Metadata
1. **`html_body`** - NULL
   - **Issue**: `markdown-to-html` is called but result not saved
   - **Fix**: Capture HTML response and update article

2. **`breadcrumb_title`** - NULL
   - **Issue**: Not generated
   - **Fix**: Generate from title (shorter version, 40-60 chars)

3. **`canonical_url`** - NULL
   - **Issue**: Not generated
   - **Fix**: Generate from site_id + slug pattern

4. **`focus_keyword`** - NULL
   - **Issue**: Not set from strategy
   - **Fix**: Use `strategy.primary_keyword` when available

#### Social Media Metadata
5. **`og_title`** - NULL
   - **Issue**: Not generated
   - **Fix**: Use title or generate optimized version

6. **`og_description`** - NULL
   - **Issue**: Not generated
   - **Fix**: Use meta_description or excerpt

7. **`og_image`** - NULL
   - **Issue**: Not set even when featured_image_url exists
   - **Fix**: Copy from featured_image_url

8. **`twitter_title`** - NULL
   - **Issue**: Not generated
   - **Fix**: Use og_title or title

9. **`twitter_description`** - NULL
   - **Issue**: Not generated
   - **Fix**: Use og_description or excerpt

10. **`twitter_image`** - NULL
    - **Issue**: Not set even when featured_image_url exists
    - **Fix**: Copy from featured_image_url

#### Image Metadata
11. **`featured_image_alt`** - NULL on some articles
    - **Issue**: Not generated when image is created
    - **Fix**: Generate descriptive alt text from title

#### Content Metadata
12. **`persona`** - NULL
    - **Issue**: Not set from strategy or site config
    - **Fix**: Use site's persona or strategy persona

13. **`tags`** - NULL
    - **Issue**: Not extracted from strategy
    - **Fix**: Extract from strategy.tags or generate from content

14. **`scheduled_date`** - NULL
    - **Issue**: Not set from strategy.target_date
    - **Fix**: Use strategy.target_date if available

### ⚠️ UI-Only Fields (Not Generated by Edge Functions)
- `readability_score` - Calculated in UI only
- `seo_score` - Calculated in UI only

---

## Root Cause Analysis

### Current Workflow Issues

1. **HTML Conversion Not Persisted**
   ```typescript
   // Current code calls markdown-to-html but doesn't save result
   const htmlResponse = await fetch(...);
   if (htmlResponse.ok) {
     console.log('✅ HTML conversion complete');
     // ❌ Missing: Save html_body to article
   }
   ```

2. **Metadata Generation Missing**
   - Article creation only sets basic fields
   - No function to generate comprehensive metadata
   - Strategy data not fully utilized

3. **Image Alt Text Not Set**
   - Image generation doesn't create alt text
   - Should be generated from article title

---

## Fix Plan

### Phase 1: Update `agentic-content-gen` Function

1. **Create Metadata Generation Function**
   - Generate all SEO metadata fields
   - Generate social media metadata
   - Generate canonical URL
   - Generate breadcrumb title

2. **Fix HTML Conversion**
   - Capture HTML from markdown-to-html response
   - Save to `html_body` field

3. **Enhance Article Creation**
   - Set `focus_keyword` from strategy
   - Set `persona` from site config
   - Set `tags` from strategy
   - Set `scheduled_date` from strategy
   - Generate all metadata fields

4. **Fix Image Alt Text**
   - Generate alt text when image is created
   - Save to `featured_image_alt`

### Phase 2: Create Post-Processing Function

Create `article-metadata-enhancer` function to:
- Fix existing articles with missing metadata
- Generate missing fields for all 41 articles
- Update in batch

### Phase 3: Update Batch Processor

- Ensure all metadata is passed from strategy
- Verify workflow completes all steps

---

## Implementation Priority

1. **High Priority** (Blocks deployment):
   - `html_body` - Required for rendering
   - `canonical_url` - SEO critical
   - `focus_keyword` - SEO critical
   - `og_metadata` - Social sharing
   - `twitter_metadata` - Social sharing

2. **Medium Priority**:
   - `breadcrumb_title` - UX enhancement
   - `featured_image_alt` - Accessibility
   - `persona` - Content organization
   - `tags` - Content organization

3. **Low Priority**:
   - `scheduled_date` - Can be set manually

---

## Expected Outcome

After fixes:
- ✅ All 41 articles have complete metadata
- ✅ `html_body` populated for all articles
- ✅ SEO metadata complete
- ✅ Social media metadata complete
- ✅ Images have alt text
- ✅ Articles ready for deployment




